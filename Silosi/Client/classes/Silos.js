export default class Silos {
    constructor(silosID, oznaka, kapacitet, trenutnoStanje) {
        this.silosID = silosID;
        this.oznaka = oznaka;
        this.kapacitet = kapacitet;
        this.trenutnoStanje = trenutnoStanje;
    }
}